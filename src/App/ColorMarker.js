const currentTimestamp = Date.now();
console.log(currentTimestamp);

class ColorMark {

    // if(currentTimestamp >= currentTimestamp + 259200) NB nombre de secondes en 3 jours

}

export default ColorMark